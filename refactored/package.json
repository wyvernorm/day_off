{
  "name": "shift-manager-refactored",
  "version": "2.0.0",
  "description": "Shift Manager - Refactored with Modules",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
